	[[ "$1" == "-h" ]] && {
		echo -e "
        this -h for <${FUNCNAME[0]}> ::
        doing :: giged from .p007.d:g.tst as exa 
			\$1 - 
			\$2 - 
			\$3 - 
            exa use ::
            far use ::
        "
		l_00_echo_code "exit :: <${FUNCNAME[0]}>"
		return 0
	}
	