    # from ~/fns_bsh/.d/.p.ax/.p010.d/.dta/.pr.dta/start_make.dom.d/001_body.tml

    eval "cd \${dr_pth_fn_${rnd}} || return 1"

    make clean
    make

    ./hello.cxe
    echo $?
    
    # {{002_body}}
